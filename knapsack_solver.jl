
function knapsack_solver()
    println("Hello, Git!")
end

# Let's add the problem parameters for the knapsack algorithm.

function set_parameters()
    items = [("item1", 2, 6), ("item2", 3, 4), ("item3", 4, 2)]
    capacity = 5
    return items, capacity
end

knapsack_solver()